# Independent Codex peer review — Topic 02 — Round 3

You are the fresh, independent closure reviewer for Topic 02 in:

`D:\Dev\Projects\omp-template`

The user explicitly authorized Codex as a temporary substitute because Claude/Opus has no
available account or quota. This is an adversarial, read-only review. You did not author the
snapshot and must not repair it.

## Frozen input

Read this packet first:

`D:\Dev\Projects\omp-template\codex-peer-review-packet-topic02-round3.md`

Its required SHA-256 is:

`2D809ED7ED1776CB3E5A7FCEE7C6D28D33D72942E628DD770CC670995DAE6295`

Hash it before relying on it. Then follow its mandatory read order, reproduce every
load-bearing hash and prescribed check, inspect the pinned OMP source ranges directly, and
answer all twelve mandatory questions.

Round 1 found three Important defects. Round 2 independently closed them but found another
Important defect: permanent named-role/fixed-topology authority survived in other active
documents. Treat all four findings as open until you independently falsify them against the
Round-3 snapshot.

Do not rely only on the focused validator or literal phrase searches. Audit semantic
equivalents across the active specification and phase projections, especially requirements
that make a named Verifier, exact roster, separate child session, parallel batch, isolation,
LSP, command execution, schemas, skills, aliases, or settings unconditional. At the same time,
ensure the correction did not weaken contract-required independent evidence or fail-closed
checks for capabilities selected by Topic 03.

Respect explicit history fences only after verifying them:

- `spec/03-agent-topology.md` sections B–I are pre-Topic-03 hypotheses under its top-level
  non-authority fence.
- Phase-02 content below its superseded Appendix-A heading is history.
- Phase 00 and its pinned runtime hashes are historical evidence, not current runtime
  migration.

## Read-only rules

- Do not edit, format, stage, commit, reset, clean, move, or delete any repository file.
- Do not modify the pinned OMP checkout.
- Do not reinterpret a hash mismatch as harmless.
- Do not return `INSUFFICIENT_EVIDENCE` without an actual missing artifact, reproduced
  mismatch, inaccessible required source, or internal evidence contradiction.
- The dirty worktree is expected and belongs to the user; inspect only.

## Required response

Return a substantive review containing:

1. One exact verdict token on its own line:

   `ACCEPT_TOPIC_02`, `REOPEN_TOPIC_02`, or `INSUFFICIENT_EVIDENCE`.
2. The reproduced packet hash, load-bearing hash result, repository HEAD/branch/staged count,
   pinned OMP commit/cleanliness/version, historical-pin result, validator results,
   `git diff --check` result, source-anchor result, and Phase-DAG result.
3. A disposition for R1-F1, R1-F2, R1-F3, and R2-F1.
4. Direct answers to all twelve packet questions.
5. Every Critical, Important, and Minor finding with exact file/line evidence and why it
   changes—or does not change—the approved contract.
6. An explicit statement that you made no writes.

`ACCEPT_TOPIC_02` is permitted only when no Critical or Important scoped defect remains.
Preferences for a fixed roster, universal review, mandatory parallelism, or complex Cheap Scout
optimization are not findings unless the approved contract becomes contradictory, unsafe,
unimplementable, weakened, or irreproducible.
