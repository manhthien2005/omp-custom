# Independent Codex peer review — Topic 02 — Round 6

You are the fresh, independent closure reviewer for Topic 02 in:

`D:\Dev\Projects\omp-template`

The user explicitly authorized Codex as a temporary substitute because Claude/Opus has no
available account or quota. This is an adversarial, read-only review. You did not author the
snapshot and must not repair it.

## Frozen input

Read this packet first:

`D:\Dev\Projects\omp-template\codex-peer-review-packet-topic02-round6.md`

Its required SHA-256 is:

`05C8FE205D8714FE421EDB98DC1FFFFB306317E9D529E02BC4FF1F138DA21291`

Hash it before relying on it. Follow its complete read order, reproduce every hash and check,
inspect the pinned OMP source ranges, close or reopen every prior finding, and answer all twelve
questions.

Round 5 proved that a selected-path disclosure is not fail-closed when the same contract still
runs with weaker semantics. Specifically falsify the Round-5 correction across DNA, specs
05/07/10/12/13, Phases 01/03/05/06, and the focused validator. Search semantic equivalents in
every other active authority surface too. Preserve an explicit user setting, but do not treat
that preservation as permission to execute a selected capability contract whose conjunction is
unmet.

Test these distinctions carefully:

- a non-LSP contract may validly continue; the same selected LSP-consuming contract may not
  silently or openly substitute `grep`;
- choosing a non-LSP replacement must be explicit, reconciled, and validated, with the material-
  change rule applied if a locked contract element changes;
- bash, schemas, skills, aliases, batch, and isolation may have different valid fallback shapes,
  but no selected required capability may produce false acceptance;
- bounded retrieval escalation permits disclosed named skips and does not require exhaustion;
- `.task/` safety follows isolation and artifact retention, not Standard/Orchestrated naming;
- whitespace normalization in the guard must tolerate Markdown wrapping without concealing a
  forbidden semantic.

Do not rely only on the focused validator. Forbidden strings inside its helper and mutation
fixtures are regression sentinels, not active architecture authority. Re-run the mutation suite
and inspect why each Round-5 sentinel would fail.

Respect history fences only after verifying them:

- `spec/03-agent-topology.md` sections B–I are pre-Topic-03 hypotheses.
- Phase-02 content below its superseded Appendix-A heading is history.
- Phase 00 and its pinned runtime hashes are historical evidence, not current migration.
- A research/dossier header may make that document evidence rather than decision authority;
  do not infer that status where the document does not say so.

## Read-only rules

- Do not edit, format, stage, commit, reset, clean, move, or delete any repository file.
- Do not modify the pinned OMP checkout.
- Do not reinterpret a hash mismatch as harmless.
- Do not return `INSUFFICIENT_EVIDENCE` without an actual missing artifact, mismatch,
  inaccessible source, or evidence contradiction.
- The dirty worktree belongs to the user; inspect only.

## Required response

Return a substantive review containing:

1. One exact verdict token on its own line:
   `ACCEPT_TOPIC_02`, `REOPEN_TOPIC_02`, or `INSUFFICIENT_EVIDENCE`.
2. Reproduced packet/load-bearing hashes, HEAD/branch/staged count, pinned OMP
   commit/cleanliness/version, historical pins, validators, `git diff --check`, source anchors,
   and Phase DAG.
3. Disposition for R1-F1, R1-F2, R1-F3, R2-F1/R3-F1/R4-F1/R5-F1, R5-F2, and R5-F3.
4. Direct answers to all twelve questions.
5. Every Critical, Important, and Minor finding with exact file/line evidence.
6. Explicit confirmation that you made no writes.

`ACCEPT_TOPIC_02` is permitted only when no Critical or Important scoped defect remains.
