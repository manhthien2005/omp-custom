# External research clones — tracked only via registry, not source-controlled
_research/upstreams/

# OMP session and database artifacts
*.db
*.db-shm
*.db-wal

# Editor
.vscode/
.idea/

# OS
Thumbs.db
.DS_Store

# Secrets — never commit
.env
*.key
*.pem
credentials.*
secrets.*

# Local evaluation campaign output can contain environment-specific telemetry.
evals/results/

# Local Claude Code scratch and linked worktrees
/.claude/tmp/
/.claude/worktrees/
/.worktrees/

# Phase 00 experiment copies (disposable; authoritative evidence in docs/evidence/phase-00/)
/.tmp-phase00-*/
